name: CI Pipeline
on:
    push:
        branches: [ main ]
    
    pull_request:
        branches: [ main ]

    workflow_dispatch:

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello
              run: echo "Changed files detected. Running CI pipeline..."